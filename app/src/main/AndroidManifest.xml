<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".common.AircraftApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TransparentTheme">
        <activity
            android:name=".gui.HistoryActivity"
            android:exported="false"
            android:label="@string/history_title"
            android:theme="@style/Theme.Aircraft.History" />
        <activity
            android:name=".gui.PrivacyPolicyActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".gui.AboutAircraftActivity"
            android:exported="false"
            android:label="@string/about_aircraft_title"
            android:theme="@style/Theme.Aircraft.History" />
        <activity
            android:name=".gui.DeviceInfoActivity"
            android:exported="false"
            android:label="@string/title_activity_device_info"
            android:theme="@style/Theme.Aircraft.History" />
        <activity
            android:name=".gui.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:theme="@style/Theme.Aircraft.History" />

        <activity
            android:name=".gui.PrivacyPolicyAcceptActivity"
            android:exported="true"
            android:theme="@style/Theme.Aircraft.History">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.LaunchActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".gui.MainActivity"
            android:exported="true"
            android:theme="@style/TransparentTheme" />

        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>